<h1><%= @list.name %></h1>
<%= link_to 'Add Movie', new_list_bookmark_path(@list.id) %>
<%= link_to 'Lists Home', lists_path %>


<div>
  <h2>Movies</h2>
  <div>
    <% @list.movies.each do |movie| %>
      <%= movie.title  %>
    <% end %>
  </div>
</div>
